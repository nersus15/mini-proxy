version: "3.8"

services:
  mini-proxy:
    build: .
    ports:
      - "2022:7272"
    environment:
      - APP_ENV=development
    networks:
      - webcore-network

networks:
  webcore-network:
    driver: bridge
